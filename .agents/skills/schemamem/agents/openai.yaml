interface:
  display_name: "Schemamem"
  short_description: "Repo-specific patterns and workflows for schemamem"
  default_prompt: "Use the schemamem repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true