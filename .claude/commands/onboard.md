---
description: Get oriented in the SchemaMem project
---
Bring yourself up to speed on this project, then give me a short status brief.

1. Read `CLAUDE.md` fully — especially "What is STABLE vs what is MOVING" and
   the Locked design invariants.
2. Skim `docs/design/core_model.md` for the current conceptual framing (this is
   the canonical science doc and it changes over time — treat it as current truth).
3. Look at `src/schemamem/core.py` (the deterministic arbitration engine) and
   `src/schemamem/schema_memory.py` (the public API + eval contract).
4. Run `/test` to confirm the repo is in a green state.
5. Brief me in ≤10 lines: what's implemented, what's an ablation toggle, what's
   the next open fork (see method_reflection.md), and anything currently red.
